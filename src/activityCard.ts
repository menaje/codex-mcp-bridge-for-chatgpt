import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { resolveHostUiLocaleTag, serializedUiTranslations } from "./uiI18n.js";
import { PRODUCT_INFO } from "./productInfo.js";
import {
  currentUiResourceUri,
  htmlForUiResource,
  uiResourceRevisions
} from "./uiResources.js";

export const ACTIVITY_CARD_URI = currentUiResourceUri("activity");
export const ACTIVITY_CARD_CONTRACT_GENERATION = 11;
export const RETAINED_ACTIVITY_CARD_CONTRACT_GENERATION = 6;
export const ACTIVITY_PRIVATE_METADATA_CONTRACT_VERSION = 11;
export const ACTIVITY_BOOTSTRAP_METADATA_KEY = "codex/activityBootstrap@11";
export const ACTIVITY_VIEW_METADATA_KEY = "codex/activityView@11";
export const ACTIVITY_CARD_MIME_TYPE = "text/html;profile=mcp-app";
export const ACTIVITY_CARD_RESOURCE_DESCRIPTOR = {
  title: `${PRODUCT_INFO.displayName} Activity Feed`,
  description: "Localized flat feed with current or latest Codex model and effort selections.",
  mimeType: ACTIVITY_CARD_MIME_TYPE
} as const;
export const ACTIVITY_CARD_CONTENT_METADATA = {
  ui: {
    prefersBorder: true,
    csp: { connectDomains: [] as string[], resourceDomains: [] as string[] },
    domain: "https://web-sandbox.oaiusercontent.com"
  },
  "openai/widgetDescription":
    "Shows Codex work and each Agent's current or latest model and effort selection as a flat feed.",
  "openai/widgetPrefersBorder": true,
  "openai/widgetCSP": { connect_domains: [] as string[], resource_domains: [] as string[] },
  "openai/widgetDomain": "https://web-sandbox.oaiusercontent.com",
  "codex/uiContractGeneration": ACTIVITY_CARD_CONTRACT_GENERATION
} as const;

export function registerActivityCardResource(server: McpServer): void {
  for (const [index, revision] of uiResourceRevisions("activity").entries()) {
    server.registerResource(
      index === 0 ? "codex-activity-card" : `codex-activity-card-compat-${index}`,
      revision.uri,
      ACTIVITY_CARD_RESOURCE_DESCRIPTOR,
      async () => ({
        contents: [
          {
            uri: revision.uri,
            mimeType: ACTIVITY_CARD_MIME_TYPE,
            text: htmlForUiResource("activity", revision.uri, ACTIVITY_CARD_HTML),
            _meta: {
              ...ACTIVITY_CARD_CONTENT_METADATA,
              "codex/uiContractGeneration": revision.contractGeneration ||
                (index === 0
                  ? ACTIVITY_CARD_CONTRACT_GENERATION
                  : RETAINED_ACTIVITY_CARD_CONTRACT_GENERATION)
            }
          }
        ]
      })
    );
  }
}

export const ACTIVITY_CARD_HTML = String.raw`<!doctype html>
<html lang="en" dir="auto">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Codex activity feed</title>
  <style>
    :root{color-scheme:light dark;font-family:ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;--surface:color-mix(in srgb,Canvas 97%,CanvasText 3%);--muted:color-mix(in srgb,CanvasText 62%,transparent);--faint:color-mix(in srgb,CanvasText 9%,transparent);--border:color-mix(in srgb,CanvasText 15%,transparent);--danger:#c34132;--warn:#b87503;--ok:#1a8f55;--active:#16875a;--discussion:#6373d8}*{box-sizing:border-box}[hidden]{display:none!important}body{margin:0;padding:12px;background:transparent;color:CanvasText}.card{border:1px solid var(--border);border-radius:16px;background:var(--surface);padding:14px}header,.header-title,.row-main,.actions,.footer,.disclosure{display:flex;align-items:center;gap:8px}header,.footer{justify-content:space-between}h1{margin:0;font-size:17px}.header-count,.meta,.empty,.message,.summary{color:var(--muted)}.header-count{font-size:12px;font-weight:650}.icon-button{width:30px;height:30px;padding:0;display:grid;place-items:center;font-size:16px}.list{margin-top:9px}.row{display:grid;grid-template-columns:20px minmax(0,1fr) auto;gap:9px;padding:11px 0;border-bottom:1px solid var(--border)}.row:last-child{border-bottom:0}.state-icon{position:relative;width:18px;height:18px;margin-top:1px;color:var(--muted)}.state-icon::before{content:"●";position:absolute;inset:0;display:grid;place-items:center;font-size:13px}.state-icon.running{color:var(--active)}.state-icon.attention{color:var(--warn)}.state-icon.failed{color:var(--danger)}.state-icon.completed{color:var(--ok)}.state-icon.completed::before{content:"✓";font-size:16px;font-weight:750}.state-icon.discussion{width:15px;height:11px;margin:3px 1px 0;border:1.5px solid var(--discussion);border-radius:5px;color:var(--discussion)}.state-icon.discussion::before{content:""}.state-icon.discussion::after{content:"";position:absolute;left:2px;bottom:-4px;border-width:3px 3px 0 0;border-style:solid;border-color:var(--discussion) transparent transparent transparent}.content{min-width:0}.row-main{justify-content:space-between;align-items:flex-start}.name{font-size:14px;font-weight:700;overflow-wrap:anywhere}.summary{font-size:12px;line-height:1.45;margin-top:3px}.meta{font-size:11px;line-height:1.4;margin-top:3px;overflow-wrap:anywhere}.execution-list{display:flex;flex-wrap:wrap;gap:4px 6px;margin-top:6px}.execution{max-width:100%;padding:2px 7px;border:1px solid var(--border);border-radius:999px;background:var(--faint);color:var(--muted);font-size:11px;line-height:1.35;overflow-wrap:anywhere}.actions{justify-content:flex-end;flex-wrap:wrap;max-width:220px}button{border:1px solid var(--border);border-radius:8px;background:Canvas;color:CanvasText;padding:5px 8px;font-size:11px;font-weight:650;cursor:pointer}button:hover{background:var(--faint)}button:focus-visible{outline:2px solid color-mix(in srgb,var(--discussion) 70%,transparent);outline-offset:2px}button.danger{color:var(--danger);border-color:color-mix(in srgb,var(--danger) 42%,transparent)}button:disabled{opacity:.55;cursor:wait}.groups{border-top:1px solid var(--border);margin-top:5px}.group+.group{border-top:1px solid var(--border)}.disclosure{width:100%;justify-content:space-between;border:0;border-radius:0;background:transparent;padding:11px 0;font-size:12px}.disclosure-label{display:flex;align-items:center;gap:7px}.chevron{display:inline-block;width:7px;height:7px;border-right:1.5px solid currentColor;border-bottom:1.5px solid currentColor;transform:rotate(-45deg);transition:transform .12s ease}.disclosure[aria-expanded="true"] .chevron{transform:rotate(45deg)}.group-list{padding-left:20px}.history-row .name{font-weight:650}.interaction{border-top:1px solid var(--border);margin-top:9px;padding-top:9px;display:grid;gap:7px}.interaction input{width:100%;border:1px solid var(--border);border-radius:8px;background:Canvas;color:CanvasText;padding:7px;font:inherit;font-size:11px}.interaction label,.interaction .message{font-size:11px}.footer{margin-top:10px}.message{font-size:11px}.message.error{color:var(--danger)}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}@media(max-width:520px){.card{padding:12px}.row{grid-template-columns:18px minmax(0,1fr)}.row>.actions{grid-column:2;justify-content:flex-start;max-width:none}.row-main{display:block}.row-main>.actions{margin-top:7px;justify-content:flex-start;max-width:none}.group-list{padding-left:10px}.footer{align-items:flex-start;gap:5px;flex-direction:column}}
    body{padding:0}.card{border:0;border-radius:0;background:transparent}.row{grid-template-columns:20px minmax(0,1fr)}html[data-collapsed="true"],html[data-collapsed="true"] body{height:1px!important;min-height:1px!important;max-height:1px!important;overflow:hidden!important}
  </style>
</head>
<body hidden>
  <main class="card">
    <header>
      <div class="header-title"><h1 data-i18n="activity.currentActivities"></h1><span class="header-count" id="current-count"></span></div>
      <button id="refresh" class="icon-button" type="button"><span aria-hidden="true">↻</span></button>
    </header>
    <section class="list" id="active-list"></section>
    <section class="groups" id="groups"></section>
    <div class="footer"><span class="message" id="message" role="status" aria-live="polite"></span><span class="meta" id="updated"></span></div>
  </main>
	<script>
	  const BUNDLES=${serializedUiTranslations()};
	  ${resolveHostUiLocaleTag.toString()}
	  const pending=new Map();
	  const ACTIVITY_BOOTSTRAP_METADATA_KEY=${JSON.stringify(ACTIVITY_BOOTSTRAP_METADATA_KEY)},ACTIVITY_VIEW_METADATA_KEY=${JSON.stringify(ACTIVITY_VIEW_METADATA_KEY)},MOUNTED_CARD_TOOLS=new Set(["codex_activity_snapshot","codex_activity_handoff","codex_activity_job_cancel","codex_background_process_terminate","codex_interaction_respond","codex_job_steer"]),AUTOMATIC_BOOTSTRAP_REASONS=new Set(["new-presentation","render-retry","render-latest","render-reserved","render-confirmed","active-lease"]),widgetInstanceId=crypto.randomUUID();
	  let requestId=1,snapshot=null,mountedActivityHint=null,mountedPresentationHint=null,taskBootstrapKey=null,controls={agents:[]},mounted=true,watching=false,failures=0,handoffTimer=null,viewLimit=30,standardBridgeReady=Promise.resolve(false),standardBridgeInitialized=false,sizeReportingReady=false,sizeFrame=0,sizeChangeForced=false,resizeObserver=null,lastWidth=-1,lastHeight=-1;
  const expandedGroups={completed:false,idle:false,ended:false};
  const initialMetadata=window.openai&&window.openai.toolResponseMetadata||{},initialToolInput=window.openai&&window.openai.toolInput;
  let taskInvocation=Boolean(initialToolInput&&typeof initialToolInput.prompt==="string"),taskInputRequestId=initialToolInput&&typeof initialToolInput.requestId==="string"?initialToolInput.requestId:null;
  let hostLocaleTag=resolveHostUiLocaleTag(window.openai&&window.openai.locale,initialMetadata,navigator.language),localePreference="auto",localeTag=hostLocaleTag,locale=resolveLocale(localeTag),t=BUNDLES[locale]||BUNDLES.en;
  const cardRoot=document.querySelector("main.card"),activeList=document.getElementById("active-list"),groups=document.getElementById("groups"),message=document.getElementById("message"),updated=document.getElementById("updated"),refresh=document.getElementById("refresh"),currentCount=document.getElementById("current-count");

  function resolveLocale(value){const v=String(value||"en").replaceAll("_","-").toLowerCase();if(v==="ko"||v.startsWith("ko-"))return"ko";if(v==="ja"||v.startsWith("ja-"))return"ja";if(v==="zh-hant"||/^zh-(tw|hk|mo)(-|$)/.test(v))return"zh-Hant";if(v==="zh"||v==="zh-hans"||v.startsWith("zh-"))return"zh-Hans";for(const key of["es","fr","de","pt"])if(v===key||v.startsWith(key+"-"))return key;return"en"}
  function effectiveLocaleTag(){return localePreference==="auto"?hostLocaleTag:localePreference}
  function setLocale(value,rerender=true){localeTag=String(value||"en").replaceAll("_","-");locale=resolveLocale(localeTag);t=BUNDLES[locale]||BUNDLES.en;document.documentElement.lang=localeTag;for(const item of document.querySelectorAll("[data-i18n]"))item.textContent=t[item.dataset.i18n]||BUNDLES.en[item.dataset.i18n];refresh.setAttribute("aria-label",t["common.refresh"]);refresh.setAttribute("title",t["common.refresh"]);if(rerender&&snapshot)render(snapshot,true)}
	  function rpcRequest(method,params,timeout=70000){return new Promise((resolve,reject)=>{const id=requestId++,timer=setTimeout(()=>{pending.delete(id);reject(new Error(t["common.error"]))},timeout);pending.set(id,{resolve:(value)=>{clearTimeout(timer);resolve(value)},reject:(error)=>{clearTimeout(timer);reject(error)}});window.parent.postMessage({jsonrpc:"2.0",id,method,params},"*")})}
	  function rpcNotification(method,params){window.parent.postMessage({jsonrpc:"2.0",method,params},"*")}
	  async function initializeStandardBridge(){try{const result=await rpcRequest("ui/initialize",{appInfo:{name:"codex-mcp-bridge-activity",version:"${ACTIVITY_CARD_CONTRACT_GENERATION}"},appCapabilities:{availableDisplayModes:["inline"]},protocolVersion:"2026-01-26"},5000);if(!result||typeof result.protocolVersion!=="string")return false;standardBridgeInitialized=true;document.documentElement.dataset.mcpApps="initialized";const context=result.hostContext||{};if(context.locale)hostLocaleTag=String(context.locale);rpcNotification("ui/notifications/initialized",{});if(localePreference==="auto")setLocale(hostLocaleTag);if(sizeReportingReady)scheduleSizeChanged(true);return true}catch{document.documentElement.dataset.mcpApps="fallback";return false}}
	  function mountedToolArgs(name,args){return MOUNTED_CARD_TOOLS.has(name)?Object.assign({},args,{widgetInstanceId}):args}
	  async function callTool(name,args,timeout=70000){const toolArgs=mountedToolArgs(name,args);if(window.openai&&typeof window.openai.callTool==="function")return window.openai.callTool(name,toolArgs);await standardBridgeReady;return rpcRequest("tools/call",{name,arguments:toolArgs},timeout)}
	  function privateActivityOutput(metadata){const view=metadata&&metadata[ACTIVITY_VIEW_METADATA_KEY];if(view&&view.kind==="codex/activityView"&&view.version===${ACTIVITY_PRIVATE_METADATA_CONTRACT_VERSION}&&view.purpose==="presentation-hydration-only"&&view.view&&view.correlation)return view.view;const bootstrap=metadata&&metadata[ACTIVITY_BOOTSTRAP_METADATA_KEY],activity=bootstrap&&bootstrap.activity,presentation=bootstrap&&bootstrap.presentation,correlation=bootstrap&&bootstrap.correlation,renderState=bootstrap&&bootstrap.render;if(!bootstrap||bootstrap.kind!=="codex/activityBootstrap"||bootstrap.version!==${ACTIVITY_PRIVATE_METADATA_CONTRACT_VERSION}||bootstrap.purpose!=="presentation-hydration-only"||!activity||!presentation||!correlation||!renderState)return null;return{requestId:correlation.requestId,bridgeActivity:{activityId:activity.activityId,cardGeneration:activity.cardGeneration,presentationKind:"automatic",activityPresentationId:correlation.activityPresentationId,jobId:correlation.jobId,shouldRenderActivityCard:renderState.eligible,renderReason:renderState.reason,renderTiming:renderState.timing}}}
	  function unwrap(result){let privateOutput=null;if(result&&result._meta){const responseHostLocale=result._meta.hostLocale||result._meta["webplus/i18n"];if(responseHostLocale)hostLocaleTag=String(responseHostLocale);if(result._meta.interactionControls)controls=result._meta.interactionControls;privateOutput=privateActivityOutput(result._meta)}if(result&&result.isError){const entry=Array.isArray(result.content)&&result.content.find((item)=>item&&item.type==="text");throw new Error(entry&&entry.text||t["common.error"])}return privateOutput||result&&result.structuredContent||result}
	  function intrinsicHeight(){if(document.documentElement.dataset.collapsed==="true")return 1;if(document.body.hidden)return 0;const html=document.documentElement,original=html.style.height;html.style.height="max-content";const height=Math.ceil(html.getBoundingClientRect().height);html.style.height=original;return height}
	  function emitSizeChanged(force=false){if(!sizeReportingReady)return;const compatibility=window.openai&&typeof window.openai.notifyIntrinsicHeight==="function";if(!standardBridgeInitialized&&!compatibility)return;const width=Math.ceil(window.innerWidth),height=intrinsicHeight();if(!force&&width===lastWidth&&height===lastHeight)return;lastWidth=width;lastHeight=height;if(standardBridgeInitialized)rpcNotification("ui/notifications/size-changed",{width,height});if(compatibility)window.openai.notifyIntrinsicHeight(height)}
	  function scheduleSizeChanged(force=false){sizeChangeForced=sizeChangeForced||force;if(sizeFrame)return;sizeFrame=requestAnimationFrame(()=>{sizeFrame=0;const forced=sizeChangeForced;sizeChangeForced=false;emitSizeChanged(forced)})}
	  function setCardVisible(visible){document.body.hidden=false;cardRoot.hidden=!visible;document.documentElement.dataset.collapsed=visible?"false":"true";sizeReportingReady=true;scheduleSizeChanged()}
  function node(tag,className,text){const item=document.createElement(tag);if(className)item.className=className;if(text!==undefined)item.textContent=text;return item}
  function codeLabel(group,value){return t[group+"."+value]||String(value||"")}
  function formatNumber(value){return new Intl.NumberFormat(localeTag).format(Number(value||0))}
  function formatDuration(ms){const seconds=Math.max(0,Math.floor(Number(ms||0)/1000));if(seconds<60)return new Intl.NumberFormat(localeTag,{style:"unit",unit:"second",unitDisplay:"short"}).format(seconds);const minutes=Math.floor(seconds/60);if(minutes<60)return new Intl.NumberFormat(localeTag,{style:"unit",unit:"minute",unitDisplay:"short"}).format(minutes);return new Intl.NumberFormat(localeTag,{style:"unit",unit:"hour",unitDisplay:"short"}).format(Math.floor(minutes/60))}
  function relativeTime(iso){const delta=Date.parse(iso)-Date.now(),minutes=Math.round(delta/60000);if(Math.abs(minutes)<1)return new Intl.RelativeTimeFormat(localeTag,{numeric:"auto"}).format(0,"minute");if(Math.abs(minutes)<60)return new Intl.RelativeTimeFormat(localeTag,{numeric:"auto"}).format(minutes,"minute");return new Intl.RelativeTimeFormat(localeTag,{numeric:"auto"}).format(Math.round(minutes/60),"hour")}
  function mutationId(){return crypto.randomUUID()}
  function controlFor(agentId){return(controls&&controls.agents||[]).find((item)=>item.agentId===agentId)||{}}
  function unique(values){return[...new Set(values.filter(Boolean))]}
  function stateLabel(value){if(value==="input-required")return t["activity.inputRequired"];if(value==="approval-required")return t["activity.approval"];if(value==="waiting-gpt"||value==="verification")return t["activity.workComplete"];if(value==="orphaned")return t["agent.orphaned"];if(value==="background-unavailable")return t["activity.backgroundUnavailable"];if(value==="ended")return t["activity.endedCodex"];if(value==="idle")return t["agent.idle"];return codeLabel("job",value)}
  function toneFor(row){const state=row.displayState;if(["completed","waiting-gpt","verification"].includes(state))return"completed";if(["failed","interrupted","termination-failed","orphaned","background-unavailable"].includes(state))return"failed";if(["input-required","approval-required"].includes(state))return"attention";if(["running","terminating"].includes(state))return row.kind==="discussion"?"discussion":"running";return row.kind==="discussion"?"discussion":"muted"}
  function stateIcon(row){const tone=toneFor(row),icon=node("span","state-icon "+tone);icon.setAttribute("aria-hidden","true");return icon}
  function displayAgentName(value){const name=String(value||"").trim();return/^Codex Agent [0-9a-f]{8}(?:-[0-9a-f]{4}){3}-[0-9a-f]{12}$/i.test(name)?t["activity.defaultAgent"]:name}
  function participantNames(row){const names=unique((row.agents||[]).map((agent)=>displayAgentName(agent.agentName)));if(names.length===0)return t["activity.temporaryJob"];if(names.length<=2)return names.join(" · ");return names.slice(0,2).join(" · ")+" +"+formatNumber(names.length-2)}
  function summaryText(row){const names=participantNames(row);if(row.kind==="discussion")return names+" ↔ GPT · "+formatNumber(row.counts&&row.counts.total||0)+" "+t["activity.turns"];const parts=[names];if(row.kind==="review"&&(row.displayState==="verification"||row.displayState==="waiting-gpt"))parts.push(t["activity.reviewComplete"]);else parts.push(stateLabel(row.displayState));if(row.displayState==="running"||row.displayState==="terminating")parts.push(formatDuration(row.elapsedMs));else if(row.updatedAt)parts.push(relativeTime(row.updatedAt));return unique(parts).join(" · ")}
  function metadataText(row,showWorkspace){const roles=unique((row.agents||[]).map((agent)=>agent.role));const values=[...roles,codeLabel("kind",row.kind)];if(showWorkspace)values.push(...(row.workspaceLabels||[]));if(row.kind!=="discussion"&&row.counts&&row.counts.total>1)values.push(formatNumber(row.counts.total)+" "+t["activity.jobs"]);if(row.continued)values.push(t["activity.continued"]);const background=(row.agents||[]).reduce((sum,agent)=>sum+Number(agent.backgroundProcessCount||0),0);if(background>0)values.push(t["activity.backgroundProcesses"]+" · "+formatNumber(background));return unique(values).join(" · ")}
  function executionText(execution){const selected=execution.modelDisplayName||execution.model,rerouted=execution.reroutedModelDisplayName||execution.reroutedModel,model=rerouted?selected+" → "+rerouted:selected;return model+" · "+execution.reasoningEffort}
  function appendExecutions(parent,agents,showAgentName){const available=(agents||[]).filter((agent)=>agent&&agent.execution);if(!available.length)return;const list=node("div","execution-list");for(const agent of available){const execution=agent.execution,prefix=showAgentName?displayAgentName(agent.agentName)+" · ":"",text=prefix+executionText(execution),badge=node("span","execution",text);badge.title=text;list.appendChild(badge)}parent.appendChild(list)}
  function actionButton(label,handler,className){const button=node("button",className||"",label);button.type="button";button.addEventListener("click",handler);return button}

  function renderActivityRow(row,showWorkspace){const box=node("article","row"),content=node("div","content"),main=node("div","row-main"),identity=node("div","identity"),actions=node("div","actions"),agents=row.agents||[];box.appendChild(stateIcon(row));identity.append(node("span","sr-only",stateLabel(row.displayState)),node("div","name",row.title),node("div","summary",summaryText(row)));const metadata=metadataText(row,showWorkspace);if(metadata)identity.appendChild(node("div","meta",metadata));appendExecutions(identity,agents,agents.length>1);for(const agent of agents){const control=controlFor(agent.agentId),agentName=displayAgentName(agent.agentName);if(agent.canForceStop){const stop=actionButton(t["activity.forceStop"],()=>forceAgent(row,agent,stop),"danger");stop.setAttribute("title",agentName);actions.appendChild(stop)}if((control.backgroundProcesses||[]).length){const stopBackground=actionButton(t["activity.stopBackground"],()=>terminateBackgroundProcesses(agent,control,stopBackground),"danger");stopBackground.setAttribute("title",agentName);actions.appendChild(stopBackground)}}if(row.canRequestVerification){const verify=actionButton(t["activity.verify"],()=>requestFollowUp("verify",row,verify));actions.appendChild(verify)}if(row.canRetry){const retry=actionButton(t["activity.retry"],()=>requestFollowUp("retry",row,retry));actions.appendChild(retry)}main.append(identity,actions);content.appendChild(main);for(const agent of agents){const control=controlFor(agent.agentId);for(const interaction of control.pendingInteractions||[])content.appendChild(renderInteraction(row,agent,control,interaction))}box.appendChild(content);return box}
  function renderHistoryRow(item,kind,showWorkspace){const box=node("article","row history-row"),content=node("div","content"),title=displayAgentName(item.agentName)+(item.latestActivityTitle?" · "+item.latestActivityTitle:"");const state=kind==="completed"?"completed":kind==="ended"?"failed":"idle";box.appendChild(stateIcon({displayState:state,kind:"other"}));content.append(node("span","sr-only",stateLabel(state)),node("div","name",title));const summary=[];if(item.role)summary.push(item.role);if(kind==="completed")summary.push(item.verification==="verified"?codeLabel("verification","verified"):codeLabel("lifecycle","completed"));else if(kind==="ended")summary.push(item.displayState==="archived"?codeLabel("agent","archived"):codeLabel("lifecycle",item.displayState));else summary.push(codeLabel("agent","idle"));if(showWorkspace)summary.push(...(item.workspaceLabels||[]));summary.push(relativeTime(item.updatedAt));content.appendChild(node("div","summary",unique(summary).join(" · ")));appendExecutions(content,[item],false);if(kind==="completed"&&item.activityCount>1)content.appendChild(node("div","meta",t["activity.moreActivities"]+" "+formatNumber(item.activityCount-1)));box.appendChild(content);return box}
  function groupLabel(kind,group){if(kind==="completed")return t["activity.completedCodex"]+" · "+formatNumber(group.agentCount)+" · "+t["activity.completedWork"]+" "+formatNumber(group.activityCount);const key=kind==="idle"?"activity.idleCodex":"activity.endedCodex";return t[key]+" · "+formatNumber(group.agentCount)}
  function renderGroup(kind,group,showWorkspace){if(!group||group.agentCount<1)return;const wrapper=node("section","group"),button=node("button","disclosure"),label=node("span","disclosure-label"),chevron=node("span","chevron"),panel=node("div","group-list"),panelId="group-"+kind;button.type="button";button.setAttribute("aria-expanded",String(expandedGroups[kind]));button.setAttribute("aria-controls",panelId);chevron.setAttribute("aria-hidden","true");label.append(chevron,node("span","",groupLabel(kind,group)));button.append(label);panel.id=panelId;panel.hidden=!expandedGroups[kind];for(const row of group.rows||[])panel.appendChild(renderHistoryRow(row,kind,showWorkspace));button.addEventListener("click",()=>{expandedGroups[kind]=!expandedGroups[kind];button.setAttribute("aria-expanded",String(expandedGroups[kind]));panel.hidden=!expandedGroups[kind]});wrapper.append(button,panel);groups.appendChild(wrapper)}
  function showMoreButton(){const more=actionButton(t["activity.showMore"],async()=>{more.disabled=true;viewLimit=Math.min(100,viewLimit+30);try{await reload()}catch(error){showError(error);more.disabled=false}});more.className="disclosure";return more}

		  function render(next,localeReady=false){if(!next||!next.feed||!Array.isArray(next.feed.active))return;if(next.watcherPolicy&&next.watcherPolicy.stopReason==="presentation-duplicate"){snapshot=null;setCardVisible(false);return}setCardVisible(true);snapshot=next;if(next.mountedActivity)mountedActivityHint=next.mountedActivity;if(next.mountedPresentation)mountedPresentationHint=next.mountedPresentation;localePreference=next.uiLocalePreference||"auto";if(!localeReady)setLocale(effectiveLocaleTag(),false);currentCount.textContent="· "+formatNumber(next.feed.activeCount||0);activeList.replaceChildren();const showWorkspace=Boolean(next.feed.showWorkspaceLabels);if(next.feed.active.length===0)activeList.appendChild(node("p","empty",t["activity.noCurrent"]));for(const row of next.feed.active)activeList.appendChild(renderActivityRow(row,showWorkspace));groups.replaceChildren();renderGroup("completed",next.feed.completed,showWorkspace);renderGroup("idle",next.feed.idle,showWorkspace);renderGroup("ended",next.feed.ended,showWorkspace);if(next.feed.pagination&&next.feed.pagination.hasMore&&viewLimit<100)groups.appendChild(showMoreButton());updated.textContent=t["activity.updated"]+": "+new Date(next.generatedAt).toLocaleTimeString(localeTag);message.textContent="";message.classList.remove("error");scheduleSizeChanged();if(next.watcherPolicy&&next.watcherPolicy.live===false){if(handoffTimer){clearTimeout(handoffTimer);handoffTimer=null}message.textContent=t["activity.superseded"];scheduleSizeChanged();return}scheduleHandoffs();if(!watching)void watch()}

	  function rememberToolInput(input){if(!input)return;if(typeof input.prompt==="string")taskInvocation=true;if(typeof input.requestId==="string")taskInputRequestId=input.requestId}
	  function consumeToolOutput(next){if(next&&next.feed&&Array.isArray(next.feed.active)){render(next);return true}const outputRequestId=next&&next.bridgeSession&&next.bridgeSession.requestId||next&&next.requestId;if(taskInputRequestId&&typeof outputRequestId==="string"&&taskInputRequestId!==outputRequestId){setCardVisible(false);return true}const presentation=next&&(next.bridgeActivity||next.activityTracking);if(!presentation){if(taskInvocation||next&&next.error)setCardVisible(false);return false}taskInvocation=true;const bootstrapEligible=presentation.shouldRenderActivityCard||AUTOMATIC_BOOTSTRAP_REASONS.has(presentation.renderReason);if(!bootstrapEligible){setCardVisible(false);return true}const activityId=presentation.activityId,cardGeneration=Number(presentation.cardGeneration),activityPresentationId=presentation.activityPresentationId,reservationOwnerId=presentation.jobId;if(typeof activityId!=="string"||!activityId||!Number.isInteger(cardGeneration)||cardGeneration<1||presentation.presentationKind!=="automatic"||typeof activityPresentationId!=="string"||!activityPresentationId){setCardVisible(false);return true}const key=activityPresentationId+"\u0000"+(typeof reservationOwnerId==="string"?reservationOwnerId:"");if(taskBootstrapKey===key)return true;taskBootstrapKey=key;mountedActivityHint={activityId,cardGeneration};mountedPresentationHint={kind:"automatic",activityPresentationId,...(typeof reservationOwnerId==="string"&&reservationOwnerId?{reservationOwnerId}:{})};setCardVisible(false);void reload().catch(showError);return true}

  function interactionDecisionLabel(decision){if(decision==="accept")return t["activity.approve"];if(decision==="acceptForSession")return t["activity.approveSession"];if(decision==="decline")return t["activity.decline"];return t["common.cancel"]}
  function renderInteraction(row,agent,control,interaction){const panel=node("div","interaction"),title=node("strong","",interaction.kind==="user-input"?t["activity.inputRequired"]:t["activity.approval"]);panel.append(title,node("div","message",interaction.summary));const context=[];if(interaction.cwdLabel)context.push(interaction.cwdLabel);if(interaction.grantRootLabel)context.push(interaction.grantRootLabel);if(interaction.networkContext)context.push(interaction.networkContext.protocol+"://"+interaction.networkContext.host);if(context.length)panel.appendChild(node("div","meta",unique(context).join(" · ")));if(interaction.kind!=="user-input"){const actions=node("div","actions");for(const decision of interaction.availableDecisions||[]){let button;button=actionButton(interactionDecisionLabel(decision),()=>respondInteraction(row,agent,control,interaction,{interactionDecision:decision},button),decision==="cancel"?"danger":"");actions.appendChild(button)}if(actions.childElementCount)panel.appendChild(actions);return panel}const fields=[];for(const question of interaction.questions||[]){const input=document.createElement("input"),label=node("label","message",question.question);input.type=question.isSecret?"password":"text";input.autocomplete="off";input.dataset.questionId=question.id;panel.append(label,input);fields.push(input)}const send=actionButton(t["activity.answer"],()=>{const answers={};for(const field of fields)answers[field.dataset.questionId]=[field.value];void respondInteraction(row,agent,control,interaction,{interactionAnswers:answers},send)});panel.appendChild(send);return panel}
  async function respondInteraction(row,agent,control,interaction,response,button){button.disabled=true;try{const payload=response.interactionDecision?{decision:response.interactionDecision}:{answers:response.interactionAnswers};await callTool("codex_interaction_respond",{requestId:mutationId(),jobId:control.jobId,expectedJobVersion:control.jobVersion,interactionId:interaction.interactionId,response:payload,card:cardProof()});await reload()}catch(error){showError(error);button.disabled=false}}
  async function forceAgent(row,agent,button){const control=controlFor(agent.agentId);if(!control.jobId)return;if(!confirm(t["activity.forceConfirmTitle"]+"\n\n"+t["activity.forceConfirm"]+"\n"+t["activity.partialChanges"]))return;button.disabled=true;message.textContent=t["activity.forceStopping"];try{await callTool("codex_activity_job_cancel",{requestId:mutationId(),jobId:control.jobId,expectedJobVersion:control.jobVersion,card:cardProof(),acknowledgeAffectedJobIds:control.affectedJobIds||[control.jobId]});message.textContent=t["activity.forceStopped"];await reload()}catch(error){showError(error);button.disabled=false}}
  async function terminateBackgroundProcesses(agent,control,button){if(!confirm(t["activity.backgroundConfirm"]))return;button.disabled=true;try{if(!control.agentVersion)throw new Error(t["common.error"]);const card=cardProof();for(const process of control.backgroundProcesses||[]){const result=await callTool("codex_background_process_terminate",{requestId:mutationId(),agentId:agent.agentId,expectedAgentVersion:control.agentVersion,processId:process.processId,card}),payload=result&&result.structuredContent||result;if(payload&&payload.ok===false)throw new Error(t["common.error"])}await reload()}catch(error){showError(error);button.disabled=false}}
  async function requestFollowUp(kind,row,button){button.disabled=true;const prompt=kind==="verify"?"Verify Codex Activity "+row.activityId+". Retrieve authoritative results with codex_status, inspect files, diffs, tests, and artifacts independently, then perform the exact verification transition. Do not infer success from Codex output alone.":"Retry or recover Codex Activity "+row.activityId+". Retrieve authoritative status with codex_status, explain the failure or interruption, and ask before materially changing scope.";try{await sendFollowUp(prompt);message.textContent=t["activity.followUpSent"]}catch(error){showError(error);button.disabled=false}}
	  function showError(error){message.textContent=String(error&&error.message||error);message.classList.add("error");scheduleSizeChanged()}
  function cardProof(){const mountedActivity=snapshot&&snapshot.mountedActivity||mountedActivityHint,presentation=snapshot&&snapshot.mountedPresentation||mountedPresentationHint;if(!mountedActivity||!presentation||presentation.kind==="automatic"&&!presentation.activityPresentationId)throw new Error(t["common.error"]);return{activityId:mountedActivity.activityId,generation:mountedActivity.cardGeneration,presentation:presentation.kind==="automatic"?{kind:"automatic",activityPresentationId:presentation.activityPresentationId,...(presentation.reservationOwnerId?{reservationOwnerId:presentation.reservationOwnerId}:{})}:{kind:"explicit"}}}
  async function reload(){const next=unwrap(await callTool("codex_activity_snapshot",{card:cardProof(),limit:viewLimit}));render(next)}
  async function watch(){if(!mounted||!snapshot||watching)return;watching=true;try{const next=unwrap(await callTool("codex_activity_snapshot",{card:cardProof(),limit:viewLimit,afterVersion:snapshot.scopeVersion,waitMs:55000}));failures=0;watching=false;if(mounted)render(next)}catch(error){watching=false;if(!mounted)return;failures++;if(failures>=5){message.textContent=t["activity.manualRefresh"];message.classList.add("error");return}const jitter=Math.floor(Math.random()*750);setTimeout(()=>void watch(),Math.min(30000,1000*Math.pow(2,failures))+jitter)}}
  function scheduleHandoffs(){if(handoffTimer||!snapshot||snapshot.completionHandoff!=="auto-handoff"||snapshot.watcherPolicy&&snapshot.watcherPolicy.ownsCompletionHandoff===false||!Array.isArray(snapshot.pendingHandoffs)||snapshot.pendingHandoffs.length===0)return;handoffTimer=setTimeout(()=>{handoffTimer=null;void dispatchHandoffs()},1500)}
  async function sendFollowUp(prompt){try{return await rpcRequest("ui/message",{role:"user",content:{type:"text",text:prompt}},10000)}catch(standardError){if(window.openai&&typeof window.openai.sendFollowUpMessage==="function")return window.openai.sendFollowUpMessage({prompt});throw standardError}}
  async function dispatchHandoffs(){let claimed=[],card;try{card=cardProof();if(card.presentation.kind!=="automatic")return;const claim=unwrap(await callTool("codex_activity_handoff",{action:"claim-batch",outboxIds:snapshot.pendingHandoffs.map((event)=>event.outboxId),card}));claimed=claim&&Array.isArray(claim.events)?claim.events:[];if(claimed.length===0)return;const lookup=new Map(snapshot.pendingHandoffs.map((event)=>[event.outboxId,event])),ids=claimed.map((event)=>event.activityId),jobIds=claimed.flatMap((event)=>lookup.get(event.outboxId)&&lookup.get(event.outboxId).jobIds||[]),outboxIds=claimed.map((event)=>event.outboxId);const prompt="Codex Activity completion handoff. origin="+claim.origin+"; handoffDepth="+claim.handoffDepth+"; handoffBatchId="+claim.handoffBatchId+". Activity IDs: "+ids.join(", ")+". Job IDs: "+jobIds.join(", ")+". Retrieve authoritative results with codex_status. Independently verify files, diffs, tests, and artifacts when verification is required. Do not create another automatic handoff from this handoff.";await sendFollowUp(prompt);await callTool("codex_activity_handoff",{action:"delivered-batch",outboxIds,card})}catch(error){if(claimed.length&&card)await callTool("codex_activity_handoff",{action:"release-batch",outboxIds:claimed.map((event)=>event.outboxId),card}).catch(()=>{});showError(error)}}
	  function cancelPending(reason){for(const[id,request]of pending){rpcNotification("notifications/cancelled",{requestId:id,reason});request.reject(new Error(reason))}pending.clear()}
	  window.addEventListener("message",(event)=>{if(event.source!==window.parent)return;const value=event.data;if(!value||value.jsonrpc!=="2.0")return;if(value.method==="ping"&&value.id!==undefined){window.parent.postMessage({jsonrpc:"2.0",id:value.id,result:{}},"*");return}if(value.method==="ui/resource-teardown"&&value.id!==undefined){mounted=false;watching=false;if(resizeObserver)resizeObserver.disconnect();cancelPending("Activity card unmounted");window.parent.postMessage({jsonrpc:"2.0",id:value.id,result:{}},"*");return}if(value.id!==undefined&&pending.has(value.id)){const request=pending.get(value.id);pending.delete(value.id);value.error?request.reject(new Error(value.error.message||t["common.error"])):request.resolve(value.result);return}if(value.method==="ui/notifications/tool-input"){rememberToolInput(value.params&&value.params.arguments||value.params);return}if(value.method==="ui/notifications/host-context-changed"){if(value.params&&value.params.locale){hostLocaleTag=String(value.params.locale);if(localePreference==="auto")setLocale(hostLocaleTag)}return}if(value.method==="ui/notifications/tool-result")consumeToolOutput(unwrap(value.params))},{passive:true});
	  window.addEventListener("openai:set_globals",(event)=>{const globals=event.detail&&event.detail.globals,metadata=globals&&globals.toolResponseMetadata,privateOutput=privateActivityOutput(metadata);hostLocaleTag=resolveHostUiLocaleTag(globals&&globals.locale,metadata,hostLocaleTag);if(globals&&globals.toolInput)rememberToolInput(globals.toolInput);if(metadata&&metadata.interactionControls)controls=metadata.interactionControls;if(localePreference==="auto")setLocale(hostLocaleTag);if(privateOutput)consumeToolOutput(privateOutput);else if(globals&&globals.toolOutput)consumeToolOutput(globals.toolOutput)});
	  window.addEventListener("pagehide",()=>{mounted=false;watching=false;cancelPending("Activity card unmounted")});
  window.addEventListener("pageshow",()=>{mounted=true;if(snapshot&&!watching)void reload()});
  document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible"){mounted=true;if(snapshot&&!watching)void reload()}});
	  refresh.addEventListener("click",()=>reload().catch(showError));
	  setLocale(localeTag);
	  standardBridgeReady=window.openai?Promise.resolve(false):initializeStandardBridge();
	  if(typeof ResizeObserver==="function"){resizeObserver=new ResizeObserver(()=>scheduleSizeChanged());resizeObserver.observe(document.documentElement);resizeObserver.observe(document.body)}
	  if(initialMetadata.interactionControls)controls=initialMetadata.interactionControls;
  const initialPrivateOutput=privateActivityOutput(initialMetadata);
  if(initialPrivateOutput)consumeToolOutput(initialPrivateOutput);else if(window.openai&&window.openai.toolOutput)consumeToolOutput(window.openai.toolOutput);
</script>
</body>
</html>`;
