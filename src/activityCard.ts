import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { serializedUiTranslations } from "./uiI18n.js";
import { PRODUCT_INFO } from "./productInfo.js";
import {
  currentUiResourceUri,
  htmlForUiResource,
  uiResourceRevisions
} from "./uiResources.js";

export const ACTIVITY_CARD_URI = currentUiResourceUri("activity");
export const ACTIVITY_CARD_MIME_TYPE = "text/html;profile=mcp-app";
export const ACTIVITY_CARD_RESOURCE_DESCRIPTOR = {
  title: `${PRODUCT_INFO.displayName} Agent Activity`,
  description: "Localized lightweight status and controls for bridge-managed Codex Agents.",
  mimeType: ACTIVITY_CARD_MIME_TYPE
} as const;
export const ACTIVITY_CARD_CONTENT_METADATA = {
  ui: {
    prefersBorder: true,
    csp: { connectDomains: [] as string[], resourceDomains: [] as string[] },
    domain: "https://web-sandbox.oaiusercontent.com"
  },
  "openai/widgetDescription":
    "Shows bridge-managed Codex Agents, current Activity status, and only the actions needed now.",
  "openai/widgetPrefersBorder": true,
  "openai/widgetCSP": { connect_domains: [] as string[], resource_domains: [] as string[] },
  "openai/widgetDomain": "https://web-sandbox.oaiusercontent.com"
} as const;

export function registerActivityCardResource(server: McpServer): void {
  for (const [index, revision] of uiResourceRevisions("activity").entries()) {
    server.registerResource(
      index === 0 ? "codex-activity-card" : `codex-activity-card-compat-${index}`,
      revision.uri,
      ACTIVITY_CARD_RESOURCE_DESCRIPTOR,
      async () => ({
        contents: [
          {
            uri: revision.uri,
            mimeType: ACTIVITY_CARD_MIME_TYPE,
            text: htmlForUiResource("activity", revision.uri, ACTIVITY_CARD_HTML),
            _meta: ACTIVITY_CARD_CONTENT_METADATA
          }
        ]
      })
    );
  }
}

export const ACTIVITY_CARD_HTML = String.raw`<!doctype html>
<html lang="en" dir="auto">
<head>
  <meta charset="utf-8" /><meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Codex agents</title>
  <style>
    :root{color-scheme:light dark;font-family:ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;--surface:color-mix(in srgb,Canvas 96%,CanvasText 4%);--muted:color-mix(in srgb,CanvasText 62%,transparent);--border:color-mix(in srgb,CanvasText 16%,transparent);--danger:#c34132;--warn:#b87503;--ok:#1a8f55}*{box-sizing:border-box}body{margin:0;padding:12px;background:transparent;color:CanvasText}.card{border:1px solid var(--border);border-radius:16px;background:var(--surface);padding:14px}header,.row-head,.footer,.filters,.actions{display:flex;align-items:center;gap:8px}header,.row-head,.footer{justify-content:space-between}h1{margin:0;font-size:18px}.counts{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;margin:12px 0}.count,.row{border:1px solid var(--border);border-radius:11px}.count{padding:8px}.count b{display:block;font-size:17px}.count span,.meta,.empty,.message{font-size:11px;color:var(--muted)}.filters{margin:0 0 9px;flex-wrap:wrap}.list{display:grid;gap:8px}.row{padding:10px}.identity{min-width:0}.name{font-size:14px;font-weight:700;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.short{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:10px;color:var(--muted)}.activity-name{font-size:12px;margin-top:5px;overflow-wrap:anywhere}.badges{display:flex;gap:5px;flex-wrap:wrap;margin-top:6px}.badge{font-size:10px;border:1px solid var(--border);border-radius:999px;padding:2px 7px}.badge.bad{color:var(--danger)}.badge.warn{color:var(--warn)}.badge.ok{color:var(--ok)}.timing{display:flex;gap:10px;flex-wrap:wrap;margin-top:7px}.actions{flex-wrap:wrap;justify-content:flex-end}button{border:1px solid var(--border);border-radius:9px;background:Canvas;color:CanvasText;padding:6px 9px;font-size:11px;font-weight:650;cursor:pointer}button.danger{color:var(--danger);border-color:color-mix(in srgb,var(--danger) 45%,transparent)}button:disabled{opacity:.55;cursor:wait}.interaction{border-top:1px solid var(--border);margin-top:9px;padding-top:9px;display:grid;gap:7px}.interaction input{width:100%;border:1px solid var(--border);border-radius:8px;background:Canvas;color:CanvasText;padding:7px;font:inherit;font-size:11px}.footer{margin-top:10px}.message.error{color:var(--danger)}@media(max-width:520px){.counts{grid-template-columns:repeat(2,minmax(0,1fr))}.row-head{align-items:flex-start}.row-head>.actions{max-width:42%}}
  </style>
</head>
<body><main class="card"><header><h1 data-i18n="activity.agents"></h1><button id="refresh" data-i18n="common.refresh"></button></header><section class="counts" id="counts"></section><div class="filters" id="filters"></div><section class="list" id="list"></section><div class="footer"><span class="message" id="message" role="status" aria-live="polite"></span><span class="meta" id="updated"></span></div></main>
<script>
  const BUNDLES=${serializedUiTranslations()};const pending=new Map();let requestId=1,snapshot=null,controls={agents:[]},mounted=true,watching=false,failures=0,handoffTimer=null,showIdle=false,showArchived=false,viewLimit=30;const initialMetadata=window.openai&&window.openai.toolResponseMetadata||{};let hostLocaleTag=String(window.openai&&window.openai.locale||initialMetadata.hostLocale||initialMetadata["openai/locale"]||initialMetadata["webplus/i18n"]||navigator.language||"en"),localePreference="auto",localeTag=hostLocaleTag,locale=resolveLocale(localeTag),t=BUNDLES[locale]||BUNDLES.en;
  const list=document.getElementById("list"),counts=document.getElementById("counts"),filters=document.getElementById("filters"),message=document.getElementById("message"),updated=document.getElementById("updated"),refresh=document.getElementById("refresh");
  function resolveLocale(value){const v=String(value||"en").replaceAll("_","-").toLowerCase();if(v==="ko"||v.startsWith("ko-"))return"ko";if(v==="ja"||v.startsWith("ja-"))return"ja";if(v==="zh-hant"||/^zh-(tw|hk|mo)(-|$)/.test(v))return"zh-Hant";if(v==="zh"||v==="zh-hans"||v.startsWith("zh-"))return"zh-Hans";for(const key of["es","fr","de","pt"])if(v===key||v.startsWith(key+"-"))return key;return"en"}
  function effectiveLocaleTag(){return localePreference==="auto"?hostLocaleTag:localePreference}
  function setLocale(value,rerender=true){localeTag=String(value||"en").replaceAll("_","-");locale=resolveLocale(localeTag);t=BUNDLES[locale]||BUNDLES.en;document.documentElement.lang=localeTag;for(const item of document.querySelectorAll("[data-i18n]"))item.textContent=t[item.dataset.i18n]||BUNDLES.en[item.dataset.i18n];refresh.setAttribute("aria-label",t["common.refresh"]);if(rerender&&snapshot)render(snapshot,true)}
  function rpcRequest(method,params,timeout=70000){return new Promise((resolve,reject)=>{const id=requestId++,timer=setTimeout(()=>{pending.delete(id);reject(new Error(t["common.error"]))},timeout);pending.set(id,{resolve:(value)=>{clearTimeout(timer);resolve(value)},reject:(error)=>{clearTimeout(timer);reject(error)}});window.parent.postMessage({jsonrpc:"2.0",id,method,params},"*")})}
  function callTool(name,args,timeout=70000){if(window.openai&&typeof window.openai.callTool==="function")return window.openai.callTool(name,args);return rpcRequest("tools/call",{name,arguments:args},timeout)}
  function unwrap(result){if(result&&result._meta){const responseHostLocale=result._meta.hostLocale||result._meta["webplus/i18n"];if(responseHostLocale)hostLocaleTag=String(responseHostLocale);if(result._meta.interactionControls)controls=result._meta.interactionControls}if(result&&result.isError){const entry=Array.isArray(result.content)&&result.content.find((item)=>item&&item.type==="text");throw new Error(entry&&entry.text||t["common.error"])}return result&&result.structuredContent||result}
  function node(tag,className,text){const item=document.createElement(tag);if(className)item.className=className;if(text!==undefined)item.textContent=text;return item}
  function badge(text,kind){return node("span","badge "+(kind||""),text)}
  function codeLabel(group,value){return t[group+"."+value]||String(value||"")}
  function stateLabel(value){if(value==="input-required")return t["activity.inputRequired"];if(value==="approval-required")return t["activity.approval"];if(value==="running")return t["agent.active"];if(value==="verification")return t["activity.verification"];if(value==="idle")return t["agent.idle"];if(value==="archived")return t["agent.archived"];if(value==="orphaned")return t["agent.orphaned"];return codeLabel("job",value)}
  function stateClass(value){if(value==="completed")return"ok";if(value==="running"||value==="idle")return"";if(value==="terminating"||value==="verification"||value==="approval-required"||value==="input-required")return"warn";return"bad"}
  function formatDuration(ms){const seconds=Math.max(0,Math.floor(Number(ms||0)/1000));if(seconds<60)return new Intl.NumberFormat(localeTag,{style:"unit",unit:"second",unitDisplay:"short"}).format(seconds);const minutes=Math.floor(seconds/60);if(minutes<60)return new Intl.NumberFormat(localeTag,{style:"unit",unit:"minute",unitDisplay:"short"}).format(minutes);return new Intl.NumberFormat(localeTag,{style:"unit",unit:"hour",unitDisplay:"short"}).format(Math.floor(minutes/60))}
  function relativeTime(iso){const delta=Date.parse(iso)-Date.now(),minutes=Math.round(delta/60000);if(Math.abs(minutes)<1)return new Intl.RelativeTimeFormat(localeTag,{numeric:"auto"}).format(0,"minute");if(Math.abs(minutes)<60)return new Intl.RelativeTimeFormat(localeTag,{numeric:"auto"}).format(minutes,"minute");return new Intl.RelativeTimeFormat(localeTag,{numeric:"auto"}).format(Math.round(minutes/60),"hour")}
  function mutationId(){return crypto.randomUUID()}
  function controlFor(agentId){return(controls&&controls.agents||[]).find((item)=>item.agentId===agentId)||{}}
  function render(next,localeReady=false){if(!next||!Array.isArray(next.agents))return;snapshot=next;localePreference=next.uiLocalePreference||"auto";if(!localeReady)setLocale(effectiveLocaleTag(),false);counts.replaceChildren();const aggregate=next.aggregates||{};for(const item of[["activity.running",aggregate.running||0],["activity.attention",aggregate.needsAttention||0],["activity.verification",aggregate.readyForVerification||0],["activity.failed",aggregate.failed||0]]){const box=node("div","count");box.append(node("b","",String(item[1])),node("span","",t[item[0]]));counts.appendChild(box)}renderFilters(next);list.replaceChildren();if(next.viewMode==="activity-summary")renderActivities(next);else renderAgents(next);updated.textContent=t["activity.updated"]+": "+new Date(next.generatedAt).toLocaleTimeString(localeTag);message.textContent="";message.classList.remove("error");scheduleHandoffs();if(!watching)void watch()}
  function renderFilters(next){filters.replaceChildren();if((next.agents||[]).some((item)=>item.displayState==="idle")){const idle=node("button","",t["activity.idleAgents"]);idle.setAttribute("aria-pressed",String(showIdle));idle.addEventListener("click",()=>{showIdle=!showIdle;render(snapshot,true)});filters.appendChild(idle)}if((next.archivedAgents||[]).length){const archived=node("button","",t["activity.archivedAgents"]);archived.setAttribute("aria-pressed",String(showArchived));archived.addEventListener("click",()=>{showArchived=!showArchived;render(snapshot,true)});filters.appendChild(archived)}}
  function renderAgents(next){const active=(next.agents||[]).filter((item)=>showIdle||item.displayState!=="idle"),archived=showArchived?(next.archivedAgents||[]):[],rows=active.concat(archived);if(rows.length===0&&(next.unassignedJobs||[]).length===0)list.appendChild(node("p","empty",t["activity.noAgents"]));for(const item of rows)list.appendChild(renderAgent(item));for(const item of next.unassignedJobs||[]){const box=node("article","row"),head=node("div","row-head");head.append(node("div","name",t["activity.temporaryJob"]),badge(stateLabel(item.displayState),stateClass(item.displayState)));box.append(head,node("div","activity-name",item.activityTitle||""));list.appendChild(box)}const page=next.agentPagination||{};if(viewLimit<100&&(page.hasMore||(showArchived&&page.archivedHasMore)))list.appendChild(showMoreButton())}
  function renderAgent(item){const box=node("article","row"),head=node("div","row-head"),identity=node("div","identity"),actions=node("div","actions");identity.append(node("div","name",item.agentName),node("div","short",t["activity.agentId"]+" · "+item.shortAgentId));const badges=node("div","badges");badges.appendChild(badge(stateLabel(item.displayState),stateClass(item.displayState)));if(item.backgroundProcessState==="running")badges.appendChild(badge(t["activity.backgroundProcesses"]+" · "+String(item.backgroundProcessCount),"warn"));if(item.backgroundProcessState==="unavailable")badges.appendChild(badge(t["activity.backgroundUnavailable"],"warn"));identity.appendChild(badges);const control=controlFor(item.agentId);if(item.canForceStop){const stop=node("button","danger",t["activity.forceStop"]);stop.addEventListener("click",()=>forceAgent(item,stop));actions.appendChild(stop)}if((control.backgroundProcesses||[]).length){const stopBackground=node("button","danger",t["activity.stopBackground"]);stopBackground.addEventListener("click",()=>terminateBackgroundProcesses(item,control,stopBackground));actions.appendChild(stopBackground)}if(item.canArchive){const archive=node("button","",t["activity.archive"]);archive.addEventListener("click",()=>archiveAgent(item,archive));actions.appendChild(archive)}if(item.canRestore){const restore=node("button","",t["activity.restore"]);restore.addEventListener("click",()=>mutateAgent(item,"restore",restore));actions.appendChild(restore)}head.append(identity,actions);box.appendChild(head);if(item.activityTitle)box.appendChild(node("div","activity-name",t["activity.currentActivity"]+" · "+item.activityTitle));const timing=node("div","timing meta");timing.append(node("span","",t["activity.elapsed"]+" · "+formatDuration(item.elapsedMs)),node("span","",t["activity.lastChanged"]+" · "+relativeTime(item.updatedAt)));box.appendChild(timing);if(item.displayState==="orphaned")box.appendChild(node("div","message error",t["activity.orphaned"]));for(const interaction of control.pendingInteractions||[])box.appendChild(renderInteraction(item,control,interaction));return box}
  function renderActivities(next){if((next.activities||[]).length===0)list.appendChild(node("p","empty",t["activity.empty"]));for(const item of next.activities||[]){const box=node("article","row"),head=node("div","row-head"),identity=node("div","identity"),actions=node("div","actions");identity.appendChild(node("div","name",item.title));const badges=node("div","badges");badges.append(badge(codeLabel("lifecycle",item.lifecycle),item.lifecycle==="completed"?"ok":""),badge(codeLabel("verification",item.verification),item.verification==="failed"?"bad":item.verification==="pending"?"warn":""));identity.appendChild(badges);for(const agent of next.agents||[]){if(agent.activityId===item.activityId&&agent.canForceStop){const stop=node("button","danger",t["activity.forceStop"]);stop.addEventListener("click",()=>forceAgent(agent,stop));actions.appendChild(stop)}}head.append(identity,actions);box.append(head,node("div","meta",String(item.counts&&item.counts.total||0)+" "+t["activity.jobs"]+" · "+String((item.agentIds||[]).length)+" "+t["activity.agents"]));list.appendChild(box)}if(viewLimit<100&&next.activityPagination&&next.activityPagination.hasMore)list.appendChild(showMoreButton())}
  function showMoreButton(){const more=node("button","",t["activity.showMore"]);more.addEventListener("click",async()=>{more.disabled=true;viewLimit=Math.min(100,viewLimit+30);try{await reload()}catch(error){showError(error);more.disabled=false}});return more}
  function renderInteraction(agent,control,interaction){const panel=node("div","interaction"),title=node("strong","",interaction.kind==="user-input"?t["activity.inputRequired"]:t["activity.approval"]);panel.append(title,node("div","message",interaction.summary));if(interaction.kind!=="user-input"){const actions=node("div","actions"),approve=node("button","",t["activity.approve"]),decline=node("button","",t["activity.decline"]);approve.addEventListener("click",()=>respondInteraction(agent,control,interaction,{interactionDecision:"accept"},approve));decline.addEventListener("click",()=>respondInteraction(agent,control,interaction,{interactionDecision:"decline"},decline));actions.append(approve,decline);panel.appendChild(actions);return panel}const fields=[];for(const question of interaction.questions||[]){const input=document.createElement("input"),label=node("label","message",question.question);input.type=question.isSecret?"password":"text";input.autocomplete="off";input.dataset.questionId=question.id;panel.append(label,input);fields.push(input)}const send=node("button","",t["activity.answer"]);send.addEventListener("click",()=>{const answers={};for(const field of fields)answers[field.dataset.questionId]=[field.value];void respondInteraction(agent,control,interaction,{interactionAnswers:answers},send)});panel.appendChild(send);return panel}
  async function respondInteraction(agent,control,interaction,response,button){button.disabled=true;try{await callTool("codex_activity_update",Object.assign({activityId:agent.activityId,action:"respond-interaction",jobId:control.jobId,expectedJobVersion:control.jobVersion,interactionId:interaction.interactionId},response));await reload()}catch(error){showError(error);button.disabled=false}}
  async function forceAgent(agent,button){const control=controlFor(agent.agentId);if(!control.jobId)return;if(!confirm(t["activity.forceConfirmTitle"]+"\n\n"+t["activity.forceConfirm"]+"\n"+t["activity.partialChanges"]))return;button.disabled=true;message.textContent=t["activity.forceStopping"];try{await callTool("codex_cancel",{jobId:control.jobId,expectedVersion:control.jobVersion,acknowledgeAffectedJobIds:control.affectedJobIds||[control.jobId]});message.textContent=t["activity.forceStopped"];await reload()}catch(error){showError(error);button.disabled=false}}
  async function terminateBackgroundProcesses(agent,control,button){if(!confirm(t["activity.backgroundConfirm"]))return;button.disabled=true;try{for(const process of control.backgroundProcesses||[]){const result=await callTool("codex_agent",{requestId:mutationId(),agentId:agent.agentId,action:"terminate-background-process",processId:process.processId}),payload=result&&result.structuredContent||result;if(payload&&payload.ok===false)throw new Error(t["common.error"])}await reload()}catch(error){showError(error);button.disabled=false}}
  async function archiveAgent(agent,button){if(!confirm(t["activity.archiveConfirm"]))return;await mutateAgent(agent,"archive",button)}
  async function mutateAgent(agent,action,button){button.disabled=true;try{const result=await callTool("codex_agent",{requestId:mutationId(),agentId:agent.agentId,action}),payload=result&&result.structuredContent||result;if(payload&&payload.ok===false){message.textContent=payload.code==="AGENT_BUSY"?t["activity.archiveConflict"]:payload.code==="AGENT_BACKGROUND_PROCESS"?t["activity.backgroundArchiveConflict"]:t["common.error"];message.classList.add("error");button.disabled=false;return}await reload()}catch(error){showError(error);button.disabled=false}}
  function showError(error){message.textContent=String(error&&error.message||error);message.classList.add("error")}
  function leaseArgs(){const mountedActivity=snapshot&&snapshot.mountedActivity;return mountedActivity?{mountedActivityId:mountedActivity.activityId,cardGeneration:mountedActivity.cardGeneration}:{}}
  async function reload(){const next=unwrap(await callTool("codex_status",Object.assign({activityView:true,activityLimit:viewLimit},leaseArgs())));render(next)}
  async function watch(){if(!mounted||!snapshot||watching)return;watching=true;try{const next=unwrap(await callTool("codex_status",Object.assign({activityView:true,activityLimit:viewLimit,afterVersion:snapshot.scopeVersion,waitFor:"change",waitMs:55000},leaseArgs())));failures=0;watching=false;if(mounted)render(next)}catch(error){watching=false;if(!mounted)return;failures++;if(failures>=5){message.textContent=t["activity.manualRefresh"];message.classList.add("error");return}const jitter=Math.floor(Math.random()*750);setTimeout(()=>void watch(),Math.min(30000,1000*Math.pow(2,failures))+jitter)}}
  function scheduleHandoffs(){if(handoffTimer||!snapshot||snapshot.completionHandoff!=="auto-handoff"||!Array.isArray(snapshot.pendingHandoffs)||snapshot.pendingHandoffs.length===0)return;handoffTimer=setTimeout(()=>{handoffTimer=null;void dispatchHandoffs()},1500)}
  async function sendFollowUp(prompt){try{return await rpcRequest("ui/message",{role:"user",content:{type:"text",text:prompt}},10000)}catch(standardError){if(window.openai&&typeof window.openai.sendFollowUpMessage==="function")return window.openai.sendFollowUpMessage({prompt});throw standardError}}
  async function dispatchHandoffs(){let claimed=[];try{const claim=unwrap(await callTool("codex_activity_handoff",{action:"claim-batch",outboxIds:snapshot.pendingHandoffs.map((event)=>event.outboxId)}));claimed=claim&&Array.isArray(claim.events)?claim.events:[];if(claimed.length===0)return;const lookup=new Map(snapshot.pendingHandoffs.map((event)=>[event.outboxId,event])),ids=claimed.map((event)=>event.activityId),jobIds=claimed.flatMap((event)=>lookup.get(event.outboxId)&&lookup.get(event.outboxId).jobIds||[]),outboxIds=claimed.map((event)=>event.outboxId);const prompt="Codex Activity completion handoff. origin="+claim.origin+"; handoffDepth="+claim.handoffDepth+"; handoffBatchId="+claim.handoffBatchId+". Activity IDs: "+ids.join(", ")+". Job IDs: "+jobIds.join(", ")+". Retrieve authoritative results with codex_status. Independently verify files, diffs, tests, and artifacts when verification is required. Do not create another automatic handoff from this handoff.";await sendFollowUp(prompt);await callTool("codex_activity_handoff",{action:"delivered-batch",outboxIds})}catch(error){if(claimed.length)await callTool("codex_activity_handoff",{action:"release-batch",outboxIds:claimed.map((event)=>event.outboxId)}).catch(()=>{});showError(error)}}
  window.addEventListener("message",(event)=>{if(event.source!==window.parent)return;const value=event.data;if(!value||value.jsonrpc!=="2.0")return;if(value.id!==undefined&&pending.has(value.id)){const request=pending.get(value.id);pending.delete(value.id);value.error?request.reject(new Error(value.error.message||t["common.error"])):request.resolve(value.result);return}if(value.method==="ui/notifications/tool-result")render(unwrap(value.params))},{passive:true});
  window.addEventListener("openai:set_globals",(event)=>{const globals=event.detail&&event.detail.globals,metadata=globals&&globals.toolResponseMetadata;const responseHostLocale=metadata&&(metadata.hostLocale||metadata["webplus/i18n"]||metadata["openai/locale"]);if(globals&&globals.locale)hostLocaleTag=String(globals.locale);else if(responseHostLocale)hostLocaleTag=String(responseHostLocale);if(metadata&&metadata.interactionControls)controls=metadata.interactionControls;if(localePreference==="auto")setLocale(hostLocaleTag);if(globals&&globals.toolOutput)render(globals.toolOutput)});
  window.addEventListener("pagehide",()=>{mounted=false;watching=false;for(const [id,request] of pending){window.parent.postMessage({jsonrpc:"2.0",method:"notifications/cancelled",params:{requestId:id,reason:"Activity card unmounted"}},"*");request.reject(new Error("Activity card unmounted"))}pending.clear()});window.addEventListener("pageshow",()=>{mounted=true;if(snapshot&&!watching)void reload()});document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible"){mounted=true;if(snapshot&&!watching)void reload()}});refresh.addEventListener("click",()=>reload().catch(showError));
  setLocale(localeTag);if(initialMetadata.interactionControls)controls=initialMetadata.interactionControls;if(window.openai&&window.openai.toolOutput)render(window.openai.toolOutput);else void reload().catch(showError);
</script></body></html>`;
