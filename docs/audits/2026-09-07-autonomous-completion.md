# 자율 완료 점검 — 2026-09-07

기준 소스 `b0940b0`에 이 보고서와 함께 커밋한 운영 알림·진단 보존·검증 절차 변경을 적용했다. Apple Silicon, macOS 26.6.2, Node 24.11.1, Swift 6.3.3에서 격리된 작업 트리와 임시 상태를 사용했다. 원래 작업 트리의 실행 중 `dist`, 설치된 앱, helper, 실제 인증 정보는 교체하지 않았다. 모델 과금 호출, 릴리즈/태그 게시와 배포를 수행하지 않았다.

## 검증 결과

| 검사 | 결과 | 범위와 한계 |
| --- | --- | --- |
| `npm run check` | 662 통과, 8 제외 | 빌드, development 릴리즈 정책, 전체 Node 회귀. 일반 실행에서 SDK 계약 8건은 설치 환경/별도 실행 조건으로 제외. |
| `npm run macos:check` | 75 통과, 2 제외 | 엄격한 동시성 검사 및 경고를 오류로 처리. 실제 companion/HTTPS opt-in 두 검사는 별도 조건. |
| macOS 번역 검사 | 615개 키, 9개 언어 통과 | 기존 번역을 유지하고 운영 알림 문구 추가. |
| `npm run macos:remote:check` | 1 통과 | 실제 Swift URLSession 클라이언트 ↔ production TLS/페어링 서버. 애플리케이션 데이터만 fixture. 페어링, 반복 hello, 인증된 상태 조회, 초대 재사용·잘못된 credential·인증서 거부. 물리적인 두 Mac 검사는 아님. |
| `npm run test:continuity` | 3단계, 12건 통과 | MCP discovery/재연결 4건, 공개 작업/Agent 이어가기/백엔드 고정 4건, 프로세스 재시작/스레드 재개·분기·실패 식별 4건. 실제 ChatGPT surface 평가는 아님. |
| 진단 로그 및 helper 집중 검사 | 26건 통과 | UTF-8 크기, 개수, 시간 만료, 조회 시 정리, 로그 변조 방지와 기존 redaction/감독 경로. 전체 Node 결과에도 포함. |
| SDK 일반 계약 별도 실행 | Python 5건, SDK 6건 통과; SDK 2건 제외 | 실제 SDK 0.147.0과 가짜 app-server. 장기 실행과 알려진 만료 재현은 별도 실행. |
| SDK 장기 실행 — 2026-09-06 | Python 5건, SDK 7건 통과 | 610초 작업을 포함한 616.17초 실행. 이 검증 이후 SDK 실행 어댑터는 변경하지 않았다. |
| SDK 만료 입력 재현 — 2026-09-06 | 실패, #29 유지 | 15초 안에 완료하지 못함. [원인과 재현 방법](../codex-sdk-choice.md) 참조. 이 실패를 성공이나 일반 계약 통과에 포함하지 않았다. |

현재 연결된 Secure MCP Tunnel을 통한 읽기 전용 `codex_status`도 2026-09-06과 09-07에 성공했고 경고 배열은 비어 있었다. 이 관찰은 기존 설치의 연결 경로에 대한 증거이며 새 소스의 실제 배포/모델 실행 검증은 아니다. 현재 설치가 반환한 CLI 호환성 표시도 새 소스의 호환성 검사 결과와 동일하다고 가정하지 않았다.

## 완료 처리할 기능 이슈

| 이슈 | 완료 근거 |
| --- | --- |
| #2 선행 조건 점검 | `codexRuntime`, `macosHelperServer`, `runtimeEnv`, `runtimeEnvProjectGuard`, `tunnelSetupDiscovery`, native RuntimePaths 검사에서 설치·인증·경로·권한 누락 및 비밀 제거를 검증. |
| #5 Tunnel 설정과 상태 검사 | private profile 생성/재사용, doctor/readiness, launcher 재시작과 비밀 미상속을 fixture로 검증. 기존 실제 Tunnel에서 공개 도구 연결을 확인. |
| #8 대화 연속성 smoke | 단계별 재현 명령과 실패 단계 보고를 추가. 새 작업, 정확한 이어가기, 잘못된 식별자, 프로세스 재시작을 임시 저장소로 자동 검증. |
| #10 운영과 로그 | 기존 제한된 자동 재시작/readiness/drain과 redaction에 24시간·1MiB·200건 보존 및 운영 정책을 추가. 디스크 원본 로그는 보관하지 않는 구조임을 명시. |
| #55 원격 클라이언트 | 전체 기존 원격 상태/충돌/전환/race/종료 계약과 실제 Swift↔Node HTTPS 연결을 검증. |
| #57 첫 실행 온보딩 | 단일·복수·없는 후보, 단일 붙여넣기, 안전하지 않은 키/경로 제외, 원자적 가져오기/실패 복구 및 native 파싱 검사 통과. |
| #59 Codex 중앙 서비스 | 공통 인증/계정/세션 저장소/최신 설치 정책, 계정 중복 제거, Spark·크레딧·API 표시와 native 모델 검증 통과. SDK를 기본으로 사용하는 동안에도 선택한 SDK 계정 상태를 갱신하도록 보완. |

기능 이슈의 자동 검증 완료와 공개 릴리즈 인증을 구분한다. 깨끗한 물리 Mac 설치·Gatekeeper·sleep/wake 검증은 #44/#52/#54에서 유지하고, 같은 배포 조건 때문에 완료된 기능 이슈를 중복해서 열어두지 않는다.

## 계속 열어둘 이슈

| 이슈 | 이번 진행과 남은 조건 |
| --- | --- |
| #9 ChatGPT 평가 | [대표 프롬프트·기대 결과·기록 양식](../chatgpt-evaluation.md) 추가. Desktop/iOS/Web/reopen/widget scope와 모바일/PiP/suspend 실측은 미완료. |
| #15 운영 알림 | 발행기, 60초 유예, 원인별 재시작 중복 방지, 권한/설정, 비민감 payload, 설정 이동과 메뉴 상태 구현. 4개 자동 검사 통과. 실제 Notification Center 클릭과 VoiceOver 낭독 검증은 미완료. |
| #29 SDK | Python/TypeScript 비교 기록과 만료 입력 재현 추가. 동기 승인 콜백의 만료 정지 문제 및 설치마다 전체 계약을 확인하는 승인 게이트가 남음. |
| #58 CLI 관리 UX | 설치/업데이트/복구 상태와 재진입·재시작의 관리 계약 자동 검사 통과. 모든 상태의 실제 native 화면 노출/클릭/복원 검증은 미완료. |
| #44 native 앱 | 물리 sleep/wake·네트워크 장애·VoiceOver·키보드·다크 모드 검증이 남음. |
| #54 Intel 배포 | 실제 Intel Node/native module/호스트 모드와 설치·업그레이드·복구 증거가 필요. arm64 결과로 대체하지 않음. |
| #11, #52, #53 릴리즈 | 실제 RC 후보 산출물과 깨끗한 기기 설치, 후보 기준 runbook, RC/정식 게시가 필요. 사용자 요청에 따라 배포하지 않음. |

Offscreen SwiftUI 테스트에서 접근성 요소를 안정적으로 얻지 못한 시도는 실제 UI 통과 증거로 쓰지 않았으며 해당 미완성 테스트를 제품 검사에 남기지 않았다. 기존 설치 앱을 대신 조작하여 검증한 것으로 간주하지 않는다.
