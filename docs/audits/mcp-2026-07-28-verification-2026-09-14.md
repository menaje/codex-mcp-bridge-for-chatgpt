# MCP 2026-07-28 verification — 2026-09-14

## Candidate scope

This evidence covers `codex/mcp-2026-hardening` at the time of the checks.
The branch remains on package version `0.4.1`; this verification does not
create or promote a `0.5.0` release.

The production tool surface remains current-only. The conformance runner uses
an isolated loopback process with an internal `--conformance-fixtures` CLI
flag. That flag is absent during normal CLI startup and the normal discovery test
asserts that no `test_*` fixture tool appears in `tools/list`.

## Candidate checks

| Check | Result |
| --- | --- |
| `npm ci --dry-run --ignore-scripts` | passed |
| `npm run build` | passed |
| `npm test` | passed: 81 files, 688 tests |
| `npm run macos:check` | passed: 159 tests; 2 opt-in live smokes skipped |
| `npm run app-server:compat:check` | passed: Codex App Server CLI `0.153.3`, 416 JSON and 827 TypeScript schema files |
| `npx tsx scripts/output-contract-audit.ts --check` | passed |
| `npx tsx scripts/audit-tool-guidance.ts` | passed |
| `npm run mcp:conformance` | passed: MCP conformance `0.2.0-alpha.11`, `2026-07-28`, `server-stateless`, 29/29 checks, 0 failures, 0 warnings |

The conformance scenario skips the prompts-list-change probe because the
bridge does not advertise prompts. Its test runner reports that as an expected
skip, not a warning or a failure. The fixture mode covers the suite's named
sampling-capability, response-stream, no-log, and tools-list-change probes
through the same current-only HTTP handler used by the product.

## Installed runtime observations

These are read-only checks of the already-running local bridge, whose status
reported build `71a31f7-dirty`. It is not the candidate branch above and was
not restarted, reconfigured, or replaced for this verification.

| Observation | Result |
| --- | --- |
| `GET /healthz` | 200 |
| `server/discover` with `2026-07-28` envelope | 200; only `2026-07-28` advertised; `resultType: complete`, `ttlMs: 0`, `cacheScope: private` |
| repeated `tools/list` | byte-for-byte stable across two requests |
| `resources/list` and `resources/read` | normal read succeeded with required matching `Mcp-Name` header |
| retired `initialize` | 400 / `-32022` |
| protocol-header/body mismatch | 400 / `-32020` |
| retired `ping` and `logging/setLevel` | 404 / `-32601` |
| supplied untrusted Origin | 403; absent Origin remained accepted for native/Tunnel traffic |
| `subscriptions/listen` | SSE acknowledgement observed; close and reopen both acknowledged |
| managed Tunnel status | connected; profile health was reported as passing |

## Remaining deployment gate

A real ChatGPT conversation through the Secure MCP Tunnel has not been run
against this candidate. The current Tunnel is attached to the installed bridge
and other work is active in the shared checkout, so this verification did not
restart it, retarget it, alter Connector settings, refresh a connector, or
open a card through ChatGPT.

Before merging or replacing the installed runtime, use a separate candidate
Tunnel or a planned maintenance window to verify: discovery after connector
refresh, a current tool call in a new ChatGPT conversation, a card resource
open, and the expected scope/write-approval boundary. A host incompatibility
must hold deployment; it must not reintroduce a legacy MCP route.
